//Created a component that is used during the loading state of the custom useAxios hook
import React from 'react'

const Loading = () => {
  return (
    <div>Loading</div>
  )
}

export default Loading